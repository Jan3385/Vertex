import"../chunks/DsnmJJEf.js";import{m as o,f as w,g as n,j as f,w as r,x as i}from"../chunks/D_9WaUQ2.js";import{C as b,G as F}from"../chunks/C_nVpZx7.js";import{N as t}from"../chunks/bZIZMoq3.js";function v(u){let c=!0;const e=u.getContext("webgl2");e.clearColor(.1,.2,.3,1);const y=new Float32Array([-.5,-.5,0,.5,-.5,0,0,.5,0]),d=e.createBuffer();e.bindBuffer(e.ARRAY_BUFFER,d),e.bufferData(e.ARRAY_BUFFER,y,e.STATIC_DRAW);const l=b(e,`#version 300 es
  in vec3 aPosition;

  void main() {
    gl_Position = vec4(aPosition, 1.0);
  }
  `,`#version 300 es
  precision mediump float;
  out vec4 fragColor;

  void main() {
    fragColor = vec4(1.0, 0.5, 0.2, 1.0);
  }
  `);if(!l){console.error("Failed to create shader program");return}e.useProgram(l),e.vertexAttribPointer(0,3,e.FLOAT,!1,0,0),e.enableVertexAttribArray(0);function h(){c&&(e.clear(e.COLOR_BUFFER_BIT),e.drawArrays(e.TRIANGLES,0,3),requestAnimationFrame(h))}return h(),()=>{c=!1,e.deleteBuffer(d),e.deleteProgram(l)}}var A=f('If you have no experience with vectors (and later on matrices) I recommend first reading the <a href="/my-first-triangle/vectors-and-matrices/">vectors and matrices sub-chapter</a>',1),B=f(`<h1 id="theory">Theory</h1> <hr/> <h2 id="opengl">OpenGL</h2> <p>OpenGL needs us to provide it with some information for it on how to render stuff for us to a framebuffer. It needs 3 main things which are the vertices and additional data on what to render <em>(a buffer object)</em>, a place that knows what buffers we are going to use and how <em>(a vertex array object)</em>, and some code on how to process that data into an image <em>(a shader program)</em></p> <h2 id="buffers">Buffers</h2> <p>Buffers are a place to store things on the GPU. CPU and GPU do not share the same memory (CPU RAM vs GPU VRAM). With buffers we can set and update the GPU memory which we then use to render things to our screen. We can freely send and receive data between the CPU and GPU but the process is very slow. Because of that we should limit the amount of reading/writing between the CPU and GPU as much as possible</p> <p>The GPU can have multiple parts of VRAM optimized for different tasks. Most common example is the <em>static</em>, <em>dynamic</em> and <em>stream</em> memory. <strong>Static</strong> is optimized for rare or one-time writes where as <strong>dynamic</strong> is optimized for a lot of writes/reads in a short amount of time. The <strong>stream</strong> memory is not as common but it serves us in case we are planning to do a rare or one-time write and the GPU will use the passed data just a few times before being deleted</p> <p>We can bind the buffer to multiple types of targets. These are</p> <ul><li><strong>GL_ARRAY_BUFFER</strong> - Used to store vertex attributes like positions, normals,…</li> <li><strong>GL_ELEMENT_ARRAY_BUFFER</strong> - Holds indices that reference vertex data (this will be used and explained later on)</li> <li><strong>GL_SHADER_STORAGE_BUFFER</strong> - A large general-purpose buffer for shaders</li> <li><strong>GL_UNIFORM_BUFFER</strong> - Holds uniform data (constants) that can be shared across multiple shaders and draw calls</li> <li><strong>GL_COPY_READ_BUFFER</strong> - When copying data between shaders used as source</li> <li><strong>GL_COPY_WRITE_BUFFER</strong> - When copying data between shaders used as destination</li></ul> <h3 id="binding">Binding</h3> <p>When working with buffers (and other OpenGL objects like textures etc.) we usually bind them before working with them. The workflow looks like this</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#6A737D">// GLuint bufferID = ...;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">glBindBuffer</span><span style="color:#E1E4E8">(GL_ARRAY_BUFFER, bufferID);</span></span></code></pre></div> <p>This will “bind” the buffer represented by <code>bufferID</code> to <code>GL_ARRAY_BUFFER</code>. This means that from now on anytime we reference <code>GL_ARRAY_BUFFER</code> we are going to be referencing the buffer represented by <code>bufferID</code></p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glBufferData</span><span style="color:#E1E4E8">(GL_ARRAY_BUFFER, ..., ..., ...);</span></span></code></pre></div> <p>Now we are doing some operation with a buffer. Rather than passing <code>bufferID</code> anywhere we are instead telling OpenGL that the buffer we want to work with is bound to <code>GL_ARRAY_BUFFER</code></p> <p>After we are done with the buffer we should (but don’t have to) unbind it</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glBindBuffer</span><span style="color:#E1E4E8">(GL_ARRAY_BUFFER, </span><span style="color:#79B8FF">0</span><span style="color:#E1E4E8">);</span></span></code></pre></div> <p>This can be done by binding a <code>0</code> to that specific buffer. Now any operation with <code>GL_ARRAY_BUFFER</code> will fail and give an error instead of the alternative of changing our buffer somewhere where we don’t want to. If we do not unbind our buffers it can be very difficult to debug where and why is our buffer changing as it can be in a part of code which has nothing to do with our buffer</p> <h2 id="shaders">Shaders</h2> <p>Shaders are an OpenGL object that holds our <code>GLSL</code> code. Our code starts as a text string that then gets compiled into several shaders based on their purpose and then connects them as a shader program</p> <p>A shader program is a collection of shaders that defines the “pipeline” on how to work and process input data to pixels on the screen or for a frame buffer</p> <p>OpenGL has <em>two</em> main shaders types, the <strong>vertex shader</strong> and the <strong>fragment shader</strong>. Vertex shader tells us where are we going to be rendering on the screen where as the fragment shader fills a defined region from the vertex shader with color</p> <p>There are more shader types such as</p> <ul><li><strong>Geometry shader</strong> - Can create or discard geometry on the GPU. You can picture it as instead of passing whole triangle vertices to the GPU we just send it individual points where we want our triangles placed and it generates them for us</li> <li><strong>Tesselation control shader</strong> - Subdivides geometry</li> <li><strong>Tesselation evaluation shader</strong> - Decides the position of vertices after tesselation</li> <li><strong>Compute shader</strong> - Is not used for rendering but can be used to compute anything with the power of the GPU</li></ul> <p>Geometry and tesselation shaders are part of the <em>primitive assembly process</em></p> <p>These additional shader types are too complicated and not needed for a simple project so we will not be going though them. Any shader type that is worth mentioning is the <em>geometry shader</em> as it is not that hard to use and can prove itself very useful</p> <h3 id="vertex-shader">Vertex Shader</h3> <p>Vertex shader is at the start of our shader chain and takes in our input data vertices. If we do not want to move them in any way we can just pass them directly to the output but if we have a camera or we want to be able to change the objects position/rotation/scale we can do this during this step</p> <h3 id="fragment-shader">Fragment Shader</h3> <p>Before this shader we can put a tesselation and/or geometry shaders that modify our vertex shader output. This can be done for example to increase the level of detail or to automatically generate/modify new geometry on the GPU</p> <p>Right before running the fragment shader a process called rasterization happens. Rasterization is a process that “selects” all the pixels that are inside the area designated by vertices. The fragment shader then runs for every pixel inside the rasterization zone</p> <p>Fragment shader colors in the object. If we don’t need any special color effects we can just pass a color straight without any modifications but later on we will want to modify the color either with lights, a settable uniform variable etc.</p> <h3 id="shader-program">Shader program</h3> <p>Shader program is the pipeline of shaders used to generate an image. It takes in data from buffer(s) with the help of a vertex array object, processes it and the output is put onto our framebuffer/window</p> <p><img src="../images/OpenGL-Pipeline.png" alt="OpenGL pipeline"/><sup><a href="https://en.wikibooks.org/wiki/GLSL_Programming/OpenGL_ES_2.0_Pipeline">Image source from WikiBooks.org</a></sup></p> <h1 id="rendering">Rendering</h1> <hr/> <!> <p>To render our first triangle we must create an array of vertices representing our shape. A simple triangle may look like the following</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">float</span><span style="color:#E1E4E8"> triangleVertices[] </span><span style="color:#F97583">=</span><span style="color:#E1E4E8"> [</span></span>
<span class="line"><span style="color:#6A737D">//   X ,    Y ,   Z</span></span>
<span class="line"><span style="color:#F97583">  -</span><span style="color:#79B8FF">0.5</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">-</span><span style="color:#79B8FF">0.5</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">0.0</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">   0.5</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">-</span><span style="color:#79B8FF">0.5</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">0.0</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">,</span></span>
<span class="line"><span style="color:#79B8FF">   0.0</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">,  </span><span style="color:#79B8FF">0.5</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">0.0</span><span style="color:#F97583">f</span></span>
<span class="line"><span style="color:#E1E4E8">]</span></span></code></pre></div> <h2 id="vertex-array-buffer">Vertex Array Buffer</h2> <p>Vertex Array Buffer (VBO) will help us send our vertices over to the GPU memory. Generating our buffer is simple</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#E1E4E8">GLuint VBO;</span></span>
<span class="line"><span style="color:#B392F0">glGenBuffers</span><span style="color:#E1E4E8">(</span><span style="color:#79B8FF">1</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">&#x26;</span><span style="color:#E1E4E8">VBO);</span></span></code></pre></div> <!> <p>To start using our buffer we need to bind it to the correct buffer slot</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glBindBuffer</span><span style="color:#E1E4E8">(GL_ARRAY_BUFFER, VBO);</span></span></code></pre></div> <p><em>GL_ARRAY_BUFFER</em> represents the slot our buffer will be slotted in. Some of the other buffer types are <em>GL_ELEMENT_ARRAY_BUFFER</em>, <em>GL_ELEMENT_ARRAY_BUFFER</em>, <em>GL_UNIFORM_BUFFER</em></p> <p>Now we need to send our CPU data <code>triangleVertices</code> to the GPU buffer <code>VBO</code></p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glBufferData</span><span style="color:#E1E4E8">(GL_ARRAY_BUFFER, </span><span style="color:#F97583">sizeof</span><span style="color:#E1E4E8">(triangleVertices), triangleVertices, GL_STATIC_DRAW);</span></span></code></pre></div> <p>GL_STATIC_DRAW has alternatives like GL_DYNAMIC_DRAW and GL_STREAM_DRAW. This will prefer to place our buffer in memory thats optimized for our amount of access</p> <ul><li><strong>GL_STATIC_DRAW</strong> means we will be rarely or never sending any more data to the GPU buffer</li> <li><strong>GL_DYNAMIC_DRAW</strong> means we will be updating our data quite often</li> <li><strong>GL_STREAM_DRAW</strong> means we will rarely or never send any more data to the GPU buffer and our GPU will use our buffer just a few times</li></ul> <p>The arguments passed to <code>glBufferData(..)</code> basically mean we will be working with an <code>array buffer</code> and we will send <code>sizeof(triangleVertices)</code> amount of data that starts at <code>triangleVertices</code> and we won’t be sending more data to the buffer anymore as it is <code>static draw</code></p> <!> <p>Now our GPU knows about our triangle’s location. All we need to do now is to set up our shaders and render it</p> <h2 id="shaders-1">Shaders</h2> <p>Shaders allow us to use our vertex array buffer in order to render something to the viewport. We need to define two shaders, A vertex and fragment shader. We can use more shaders like the geometry shader between them but we do not have to do that for now</p> <p>A shader is written using the <code>GLSL</code> programming language</p> <h3 id="vertex-shader-1">Vertex shader</h3> <div class="code-wrapper"><div class="code-label">glsl</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#E1E4E8">#version </span><span style="color:#79B8FF">330</span><span style="color:#E1E4E8"> core</span></span>
<span class="line"><span style="color:#B392F0">layout</span><span style="color:#E1E4E8"> (location </span><span style="color:#F97583">=</span><span style="color:#79B8FF"> 0</span><span style="color:#E1E4E8">) </span><span style="color:#F97583">in</span><span style="color:#F97583"> vec3</span><span style="color:#E1E4E8"> vertexPos;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">void</span><span style="color:#B392F0"> main</span><span style="color:#E1E4E8">()</span></span>
<span class="line"><span style="color:#E1E4E8">&#123;</span></span>
<span class="line"><span style="color:#E1E4E8">    gl_Position </span><span style="color:#F97583">=</span><span style="color:#B392F0"> vec4</span><span style="color:#E1E4E8">(vertexPos.x, vertexPos.y, vertexPos.z, </span><span style="color:#79B8FF">1.0</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#E1E4E8">&#125;</span></span></code></pre></div> <p>In the following example we are creating a shader for the OpenGL version 3.3. Then we are declaring that we will be binding our buffer with vertices as the first input (we will be getting into how this exactly works later). The <code>main</code> function then runs for all vertices provided where it just sets the position of our vertex to the one provided</p> <!> <h3 id="fragment-shader-1">Fragment shader</h3> <div class="code-wrapper"><div class="code-label">glsl</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#E1E4E8">#version </span><span style="color:#79B8FF">330</span><span style="color:#E1E4E8"> core</span></span>
<span class="line"><span style="color:#F97583">out</span><span style="color:#F97583"> vec4</span><span style="color:#E1E4E8"> FragColor;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">void</span><span style="color:#B392F0"> main</span><span style="color:#E1E4E8">()</span></span>
<span class="line"><span style="color:#E1E4E8">&#123;</span></span>
<span class="line"><span style="color:#E1E4E8">    FragColor </span><span style="color:#F97583">=</span><span style="color:#B392F0"> vec4</span><span style="color:#E1E4E8">(</span><span style="color:#79B8FF">1.0</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">0.5</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">0.2</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">1.0</span><span style="color:#F97583">f</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#E1E4E8">&#125; </span></span></code></pre></div> <p>A fragment shader does not interact with the bound buffers directly. It takes in variables passed from the vertex shader and runs the <code>main</code> function for each pixel that was included during <strong>rasterization</strong></p> <p>It outputs <code>FragColor</code> as an RGBA value in the range (0.0, 1.0) which will be written into our frame buffer. We can rename the <code>FragColor</code> to anything we want we just need to make it the first variable that goes out. The depth buffer is calculated automatically so we do not need to worry about it</p> <p>If the fragment shaders finds that the pixel is further away than the pixel that was in the depth buffer it is skipped</p> <h3 id="compiling">Compiling</h3> <p>Now that we have our shaders we need to get them into our C/C++ program. To do that we can either write and store it directly in our code like</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">const</span><span style="color:#F97583"> char*</span><span style="color:#E1E4E8"> vertexShaderSource </span><span style="color:#F97583">=</span><span style="color:#9ECBFF"> "..."</span><span style="color:#E1E4E8">;</span></span></code></pre></div> <p>or load it from a file using <code>std::ifstream</code> or similar</p> <!> <p>or any other way to get it into the <code>const char*</code> or <code>std::string</code> in our program memory</p> <!> <p>Now that the shader is stored in a variable in program memory we can start the compilation process</p> <p>To compile the shaders we first need to make an OpenGL object that hold the compiled shader</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#6A737D">// Already loaded GLSL code</span></span>
<span class="line"><span style="color:#6A737D">// const char* vertexShaderSource = "...";</span></span>
<span class="line"><span style="color:#6A737D">// const char* fragmentShaderSource = "...";</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8">GLuint myVertexShader;</span></span>
<span class="line"><span style="color:#E1E4E8">myShader </span><span style="color:#F97583">=</span><span style="color:#B392F0"> glCreateShader</span><span style="color:#E1E4E8">(GL_VERTEX_SHADER);</span></span>
<span class="line"><span style="color:#B392F0">glShaderSource</span><span style="color:#E1E4E8">(myVertexShader, </span><span style="color:#79B8FF">1</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">&#x26;</span><span style="color:#E1E4E8">vertexShaderSource, </span><span style="color:#79B8FF">nullptr</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#B392F0">glCompileShader</span><span style="color:#E1E4E8">(myVertexShader);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8">GLuint myFragmentShader;</span></span>
<span class="line"><span style="color:#E1E4E8">myShader </span><span style="color:#F97583">=</span><span style="color:#B392F0"> glCreateShader</span><span style="color:#E1E4E8">(GL_FRAGMENT_SHADER);</span></span>
<span class="line"><span style="color:#B392F0">glShaderSource</span><span style="color:#E1E4E8">(myVertexShader, </span><span style="color:#79B8FF">1</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">&#x26;</span><span style="color:#E1E4E8">vertexShaderSource, </span><span style="color:#79B8FF">nullptr</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#B392F0">glCompileShader</span><span style="color:#E1E4E8">(myVertexShader);</span></span></code></pre></div> <!> <p>After compiling each of our shaders we may want to look for any compilation errors. There is an OpenGL function called <code>glGetShaderiv(...)</code> exactly for this purpose</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#6A737D">// myShader will be for myVertexShader and myFragmentShader in this example</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F97583">constexpr</span><span style="color:#F97583"> unsigned</span><span style="color:#F97583"> int</span><span style="color:#E1E4E8"> MAX_MSG_ERROR_LEN </span><span style="color:#F97583">=</span><span style="color:#79B8FF"> 512</span><span style="color:#E1E4E8">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#E1E4E8">GLint compilationSuccessful;</span></span>
<span class="line"><span style="color:#B392F0">glGetShaderiv</span><span style="color:#E1E4E8">(myShader, GL_COMPILE_STATUS, </span><span style="color:#F97583">&#x26;</span><span style="color:#E1E4E8">success);</span></span>
<span class="line"><span style="color:#F97583">if</span><span style="color:#E1E4E8">(</span><span style="color:#F97583">!</span><span style="color:#E1E4E8">success)&#123;</span></span>
<span class="line"><span style="color:#F97583">  char</span><span style="color:#E1E4E8"> errorMessage[MAX_MSG_ERROR_LEN];</span></span>
<span class="line"><span style="color:#B392F0">  glGetShaderInfoLog</span><span style="color:#E1E4E8">(myShader, MAX_MSG_ERROR_LEN, </span><span style="color:#79B8FF">NULL</span><span style="color:#E1E4E8">, errorMessage);</span></span>
<span class="line"><span style="color:#B392F0">  std</span><span style="color:#E1E4E8">::cout </span><span style="color:#F97583">&#x3C;&#x3C;</span><span style="color:#9ECBFF"> "OpenGL Compilation error: "</span><span style="color:#F97583"> &#x3C;&#x3C;</span><span style="color:#E1E4E8"> errorMessage </span><span style="color:#F97583">&#x3C;&#x3C;</span><span style="color:#B392F0"> std</span><span style="color:#E1E4E8">::endl;</span></span>
<span class="line"><span style="color:#E1E4E8">&#125;</span></span></code></pre></div> <p>After compiling both (or more) shaders we need to create a <strong>shader program</strong>. Shader program combines the shaders together and allows us to use them</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#E1E4E8">GLuint shaderProgram;</span></span>
<span class="line"><span style="color:#E1E4E8">shaderProgram </span><span style="color:#F97583">=</span><span style="color:#B392F0"> glCreateProgram</span><span style="color:#E1E4E8">();</span></span></code></pre></div> <p>After creating the shader program we need to link the vertex and fragment shader to it</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glAttachShader</span><span style="color:#E1E4E8">(shaderProgram, myVertexShader);</span></span>
<span class="line"><span style="color:#B392F0">glAttachShader</span><span style="color:#E1E4E8">(shaderProgram, myFragmentShader);</span></span>
<span class="line"><span style="color:#B392F0">glLinkProgram</span><span style="color:#E1E4E8">(shaderProgram);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D">// We can again check for any errors during linking with</span></span>
<span class="line"><span style="color:#6A737D">// glGetShaderiv(shaderProgram, GL_LINK_STATUS, &#x26;success);</span></span>
<span class="line"><span style="color:#6A737D">// the rest is the same</span></span></code></pre></div> <p>Now after linking the shaders to a shader program, we do not need the shaders anymore. To free up some space and prevent memory leaks we should delete them with</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glDeleteShader</span><span style="color:#E1E4E8">(myVertexShader);</span></span>
<span class="line"><span style="color:#B392F0">glDeleteShader</span><span style="color:#E1E4E8">(myFragmentShader);  </span></span></code></pre></div> <h2 id="putting-it-all-together">Putting it all together</h2> <p>Right now we have the buffer and shader program ready. The last step is telling OpenGL how the buffer should interact with the shader program</p> <h3 id="telling-our-buffer-how-to-put-data-to-our-shader">Telling our buffer how to put data to our shader</h3> <p>To store the information on how to use our buffer we need to create a Vertex array object. It is possible with</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#F97583">unsigned</span><span style="color:#F97583"> int</span><span style="color:#E1E4E8"> VAO;</span></span>
<span class="line"><span style="color:#B392F0">glGenVertexArrays</span><span style="color:#E1E4E8">(</span><span style="color:#79B8FF">1</span><span style="color:#E1E4E8">, </span><span style="color:#F97583">&#x26;</span><span style="color:#E1E4E8">VAO);  </span></span></code></pre></div> <p>To setup the VAO we do</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#6A737D">// We are now saying our VAO how to bind our buffer and its attributes</span></span>
<span class="line"><span style="color:#B392F0">glBindVertexArray</span><span style="color:#E1E4E8">(VAO);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6A737D">// Our VAO is using VBO array buffer</span></span>
<span class="line"><span style="color:#B392F0">glBindBuffer</span><span style="color:#E1E4E8">(GL_ARRAY_BUFFER, VBO);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#B392F0">glVertexAttribPointer</span><span style="color:#E1E4E8">(</span><span style="color:#79B8FF">0</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">3</span><span style="color:#E1E4E8">, GL_FLOAT, GL_FALSE, </span><span style="color:#79B8FF">3</span><span style="color:#F97583"> *</span><span style="color:#F97583"> sizeof</span><span style="color:#E1E4E8">(</span><span style="color:#F97583">float</span><span style="color:#E1E4E8">), (</span><span style="color:#F97583">void*</span><span style="color:#E1E4E8">)</span><span style="color:#79B8FF">0</span><span style="color:#E1E4E8">);</span></span>
<span class="line"><span style="color:#B392F0">glEnableVertexAttribArray</span><span style="color:#E1E4E8">(</span><span style="color:#79B8FF">0</span><span style="color:#E1E4E8">);</span></span></code></pre></div> <p>Now lets talk about what the specific arguments mean</p> <p>In the first macro the following arguments are</p> <ul><li><strong>Vertex attribute</strong> we are modifying (it is the (location = 0) from our vertex shader)</li> <li>The <strong>size</strong> of our attribute. We are using Vec3 for the position of a vertex which is composed from 3 floats</li> <li>What <strong>data type</strong> is the attribute. A Vec3 is made from 3 <strong>floats</strong> so we use <code>GL_FLOAT</code></li> <li>If we want our data to be normalized (meaning transformed into (0; 1) or (-1; 1)). We do not want that for our position data so we set it to <code>GL_FALSE</code></li> <li>Defines the <strong>stride</strong>. Stride means how many bytes we want to skip for the next position data. We want to skip 3 floats (one Vec3 argument) to get the new one</li> <li>Defines the <strong>offset</strong> as a <code>void*</code> argument. The vertex data starts right at the start of our buffer so we are not setting any offset (thus (void*)0)</li></ul> <p>The <code>glEnableVertexAttribArray</code> tells OpenGL that we will need (layout = 0) enabled and to make it accessible</p> <h2 id="putting-a-triangle-on-the-screen">Putting a triangle on the screen</h2> <p>We have a shader program and VAO. These are the two primitives needed for rendering</p> <p>Shader program says how are we going to render the object</p> <p>VAO tells us how the vertices are stored, where they are stored and how to send it to the shader program</p> <div class="code-wrapper"><div class="code-label">cpp</div><pre class="shiki github-dark" style="background-color:#24292e;color:#e1e4e8" tabindex="0"><code><span class="line"><span style="color:#B392F0">glUseProgram</span><span style="color:#E1E4E8">(shaderProgram);</span></span>
<span class="line"><span style="color:#B392F0">glBindVertexArray</span><span style="color:#E1E4E8">(VAO);</span></span>
<span class="line"><span style="color:#B392F0">glDrawArrays</span><span style="color:#E1E4E8">(GL_TRIANGLES, </span><span style="color:#79B8FF">0</span><span style="color:#E1E4E8">, </span><span style="color:#79B8FF">3</span><span style="color:#E1E4E8">);</span></span></code></pre></div> <p><code>glDrawArrays</code> tells OpenGL to use the currently bound VAO and shader program to draw on the currently bound frame buffer (default is our window). We are telling OpenGL to draw a triangle that should start from the beginning (0) and read 3 Vec3 values (3)</p> <p>Running the code above in the rendering loop will show us a triangle on the screen. Your screen should look like the rendered element below</p> <!>`,1);function R(u){var c=B(),e=o(w(c),74);t(e,{children:(s,p)=>{r();var a=A();r(),n(s,a)}});var y=o(e,12);t(y,{children:(s,p)=>{r();var a=i("When generating an OpenGL resource, you may see an argument that is usually a '1', This is because OpenGL allows us to create multiple OpenGL resources with a single API call. The performance from this is negligible so we won't be using it");n(s,a)}});var d=o(y,18);t(d,{children:(s,p)=>{r();var a=i("As `triangleVertices` is a C/C++ array we can also use it as a pointer to the start of our data");n(s,a)}});var g=o(d,16);t(g,{children:(s,p)=>{r();var a=i("The reason for us having to set `gl_Position` to the `vertexPos` instead of this happening automatically is that commonly we would want to position/rotate/scale our vertices based on our camera or our objects transform");n(s,a)}});var E=o(g,20);t(E,{children:(s,p)=>{r();var a=i("When loading from a file it is possible to make a simple preprocessor for self-defined macros. For that you just need to look for a specific keyword like `#GET` or any other macro you define yourself and replace the line with anything you want. It is also possible to automatically insert the OpenGL version at the start of a loaded file so that we don't have to put it inside every shader we make");n(s,a)}});var l=o(E,4);t(l,{children:(s,p)=>{r();var a=i("Technically we **need** just `const char*` but we can still use `std::string` thanks to the `std::string` member function `.c_str()`");n(s,a)}});var h=o(l,8);t(h,{children:(s,p)=>{r();var a=i("the `nullptr` inside `glShaderSource` is for a specified length of our string. It is not needed as the string will contain an null terminator");n(s,a)}});var m=o(h,54);F(m,{get sketch(){return v}}),n(u,c)}export{R as component};
